## 修了什么

### 下载：CDN 不支持 Range 时不再报错中断

之前如果播放地址所在节点不接受分块下载（Range 请求），脚本会在抛错后**完全不下**，只在界面上留一行红字 `CDN 不支持 Range`——内置的单线程兜底代码写了，却因为错误被原样抛给上层而永远走不到。

现在会**就地降级为单线程**继续下载。同时探测改成「206 + content-range 总长」双条件判定，任一条不满足即认定该节点不吃 Range，并把原因带进日志（如 `CDN 不支持 Range(HTTP 200)`），便于定位。

### 下载：自动切换 CDN 镜像（本次真正的重头）

B 站每条音视频轨会返回 1 个首选地址（`baseUrl`）加若干个备用地址（`backupUrl`）。实测发现**首选地址经常是 P2P 节点**，用真实视频验证时 5 个候选里有 3 个直接连接重置（`ECONNRESET`），而真正可用的节点就躺在备用地址里——但脚本此前只认首选地址，等于开局就押注在最差的一条上。

现在下载流程分两层兜底：

1. **换镜像**：逐个尝试全部候选地址，哪个通用哪个
2. **降并发**：同一节点失败时逐步降低并发数再试（8 → 4 → 2 → 单线程）
3. **降级单线程**：节点不支持分块下载时直接转单线程，不浪费时间空转降档

音轨（含「仅下载音频」模式）同样支持镜像切换。

## 其他

- 新增两个离线回归测试并入质量门（现共 11 项）：下载鲁棒性 32 项断言、设置页更新链路 24 项断言。二者均打桩网络层，不依赖外网与浏览器，可单独运行
- 修正真机测试的报错过滤规则：此前会把 B 站自家站点脚本（`s1.hdslb.com` 等）的报错误判为本脚本问题；现改为带 `[BilibiliRSS]` 前缀的日志一律保留，其余站点噪声排除
- 版本号断言改为从产物 `@version` 动态读取，避免每次发版都要改测试

## 安装 / 更新

- 全新安装：点下方 `BilibiliRSS.user.js`，油猴会提示安装
- 已装 v0.3.9：脚本会自动更新（v0.3.8 及更早版本需手动覆盖安装一次，之后即可自动更新）
- 也可在脚本面板「设置 → BilibiliRSS」卡片点「更新」按钮直接拉取最新版
