# BilibiliRSS v0.4.1

一次以**代码质量**为主题的版本：清死代码、降耦合、提响应效率。功能行为与 v0.4.0 保持一致，可以放心覆盖升级。

## 优化

**面板响应更快。** 后端数据层原先每改一个字段都调全量刷新，会把「稍后再看 / 监控 / 订阅 / 下载」四个页签统统标脏后逐个重算 —— 改的明明只是下载进度，另外三个页签也在陪跑。现在数据层只标自己真正影响的视图：

| 操作 | 原先 | 现在 |
| --- | --- | --- |
| 下载进度 / 入队 / 重试 | 四页签全刷 | 只刷下载页 |
| 订阅增删改 | 四页签全刷 | 订阅页 + 稍后再看 |
| 监控增删 / 刷新 | 四页签全刷 | 只刷监控页 |
| 初始化 / 导入 / 清空 / 全量刷新 | 四页签全刷 | 保持全刷（本就该全刷） |

47 处调用里 **39 处收窄为定向**。侧栏那几个计数角标由 `flushCounts()` 每帧无条件刷新，所以不用担心漏更新。

**渲染调度改为页签查表。** 原先是 5 个 `if` 逐个比对当前页签，现在是一张 `页签 → 渲染函数` 映射表，一眼能看出谁归谁管。

## 清理

移除 **6 处无引用代码**（扫描确认零引用，且扫描器自检无误报）：

- `subTextOf` — 订阅副标题，已被视图层内联渲染取代
- `toggleSub` — 订阅开关，界面实际走编辑弹窗路径
- `setItemStatus` — 条目状态机，实际由 `deleteItem` + 视图层动作按钮接管
- `QN_TIERS` — 画质档位数组，实际只用 `QN_LABEL` 映射
- `gmxRangeLen` — Range 探测的旧兼容包装，调用点已全迁到 `probeRange`
- `encName` — 合集卡片里算了却没插进模板的局部变量

另外更正了两处会误导后来者的结构问题：`_backend.js` 里标题为「引擎」的那节实际装的是**订阅引擎**（已更名并补注职责边界）；存储节顶部补上了**分层契约**说明 —— 数据层改 `store` 后调 `save()`、视图层只读不写、`updateAllUI('视图名')` 是数据层唯一允许的通知手段且应只传本次真正影响的视图。

## 质量门

质量门由 11 项扩至 **12 项**：

- **新增「定向标脏回归」**（14 项断言）：静态锁死本次优化 —— 下载链路不得残留全量调用、订阅/监控入口必须带对应视图名、无参调用只允许出现在白名单、调度器必须按页签查表。
- **冒烟测试加了探针自检**：`smoke.js` 靠替换源码标记字符串注入探针，此前若某个标记因源码改名而失配，那一步的调试信息会**静默消失**（`updateAllUI` 的探针就因签名改成变参而失效过）。现在所有标记集中成列表，任一失配直接报错。

## 升级

设置页 →「关于」卡片点**更新**，或在油猴里重新安装即可覆盖。装了 v0.3.9 及以后的版本，油猴也会自己提示更新。
