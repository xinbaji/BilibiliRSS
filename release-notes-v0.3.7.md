# BilibiliRSS v0.3.7

工作台交互打磨：关键词输入不再弹窗挡屏、空格不再被播放器抢走、合集/专辑页不再被长列表撑爆。

## 修复

- **输入关键词时敲空格，视频反而暂停了**
  在订阅关键词输入框里敲空格，字符输不进去，正在播放的视频却被暂停。根因是 B 站播放器的全局快捷键（空格=暂停/播放、方向键=快进/快退）挂在 `document` 上，而输入框里的按键照常冒泡上去被播放器接管。现在输入框聚焦期间，`keydown` / `keyup` / `keypress` 三类事件一律在捕获阶段拦下不外传 —— 空格只当空格用，方向键只当光标移动；`Enter` 直接确认、`Escape` 直接取消。

- **点「订阅该 UP」后卡片不出现、输入框点不到**
  若面板当时停在别的页签，卡片会被插进一个 `display:none` 的容器：既看不见，输入框也拿不到布局盒（浏览器会**静默拒绝**聚焦），整颗按钮看起来像失灵了。现在点订阅会自动打开抽屉并切到「工作台」页签，等布局落地后再聚焦。

## 调整

- **订阅关键词输入：全屏弹窗 → 工作台内联卡片**
  原先点「订阅该 UP」会弹一层盖满整页的遮罩，用户看不到当前视频 / UP 的信息。现在改为在工作台里就地插一张卡片 —— **位置紧贴触发它的那张卡片下方**，所见即所得；点「开始订阅」/「取消」/回车/Esc 后卡片自动移除。卡片同样会列出该 UP 已订阅过的关键词组合，避免建重复。

- **合集 / 专辑页：下载区默认折叠**
  合集、专辑动辄几十上百集，全量列表铺开会把工作台撑成一条长柱、把页面本身挡死。现在默认只显示一张**资料卡**（封面 / 名称 / 共 N 集 / 「订阅该合集」），下载列表收在「下载」按钮后面，点了才展开。展开时自动全选并同步「下载所选 (N)」计数，支持再点「收起」；展开状态会被记住 —— 缓存重建 DOM 后不会悄悄折回去。列表 HTML 仍预先渲染，展开 / 收起只切 `display`，不会重新请求接口。

## 升级说明

覆盖安装即可，配置与订阅数据全部保留。
