# Changelog

本文件记录 BilibiliRSS 的版本变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.9] — 2026-09-22

### 新增

- **设置页加入「更新」按钮**：「BilibiliRSS」卡片（GitHub 链接旁）新增更新按钮，点开即到最新版脚本地址 —— 指向远端仓库的 **latest Release 产物**，所以不用随版本改地址。在油猴里覆盖安装即可升级。
- **篡改猴自动更新**：脚本头部加入 `@updateURL` / `@downloadURL`，装上这一版之后，往后有新版本油猴会自己提示。

### 说明

- **更新地址默认挂国内镜像，但实测镜像并不加速**。本机实测（取 254835B 的脚本）：直连 github **176 KB/s** 最快；ghproxy.net 106 KB/s；ghfast.top 105 KB/s；jsdelivr 89 KB/s；gh-proxy.com 67 KB/s。12 个候选镜像里 **7 个已失效**（ghproxy.cc 证书过期、hub.gitmirror.com / raw.gitmirror 域名不存在、gh.llkk.cc / github.moeyy.xyz 超时、gitproxy.click 返回空响应、gcore.jsdelivr 连接重置）。
  所以镜像的价值在于「github 打不开时的唯一出路」，不在于快。默认走 `ghproxy.net`；想换源只改一处常量 `GH_MIRROR`，设成 `''` 即走直连。实测数据与失效清单已作为注释留在代码里。
- **⚠ 装过 0.3.8 的需要手动更新一次**：更新头是 v0.3.9 才加的，0.3.8 里没有，所以油猴不会主动提示。用设置页的「更新」按钮（或 GitHub Release 页）覆盖安装一次，之后才会走自动更新。

## [0.3.8] — 2026-09-22

### 修复

- **下载中途断掉后整个任务判死**：视频轨多线程失败会回退单线程，但**回退之后再失败就没有任何挽救** —— 一次网络抖动就红字报「网络超时」，任务直接作废。现在媒体下载阶段也会自动回队重试（至多 2 次），并且重试时**重新取一次播放地址**（`playurl` 的 CDN 链接是带时效的，排队久了会过期，重取正好一并解决）。只有大会员 / 付费 / 地区限制这类业务性错误才直接判死。

### 调整

- **多线程失败改为「逐级降并发」而不是一步退回单线程**：原先任一档失败就直接退回单线程（速度砍到 1/8，还白扔已经下好的分块）。现在按 用户线程数 → 一半 → … → 2 逐档重试，多数「并发太高被 CDN 限流」的情况降到一半就通了；各档都不行才回退单线程。
- **失败时掐掉还在跑的兄弟请求**：多线程里只要有一块判负，其余 Range 请求会被主动 `abort`。以前它们会在后台继续占着 CDN 的并发额度，和紧接着的重试抢同一批连接 —— 互相拖死，还白烧流量。同时失败后不再让残留回调继续写进度，进度条不再乱跳。
- **分块重试加退避 + 分类**：尝试次数 3 → 4，间隔 400ms / 800ms / 1600ms 递增（以前是零间隔连撞，被限流时几乎必然继续失败）。`HTTP 4xx`（403 防盗链、404 地址过期等）不再无谓重试，`408`/`429` 仍会重试。
- **「超时」不再含糊**：原来的 `timeout: 0` + `ontimeout: () => '超时'` 会把「连接被掐断」「响应没读完」统统报成一个「超时」，无法判断到底是没下文还是真超时。现在改为**停滞看门狗** —— 只在 120 秒内一个字节都没涨时才判失效，错误信息带上已收字节数（`停滞 12.3MB 无数据`）；分块错误还会带上是哪一块（`· 块 0-10357453`）。有进展就一直重置计时，所以慢链路的长下载不会被误杀。

### 说明

- 看门狗盯的是「有没有进展」，不是「总时长」。所以它不会像固定总时长那样把正在慢慢下的任务掐掉，这是刻意的。

## [0.3.7] — 2026-09-15

### 修复

- **输入关键词时空格被播放器抢走**：在订阅关键词输入框里敲空格，字符没进去，正在播放的视频反而被暂停了。根因是 B 站播放器的全局快捷键（空格=暂停/播放、方向键=快进/快退）挂在 `document` 上，而输入框的事件照常冒泡上去。现在输入框聚焦时，`keydown` / `keyup` / `keypress` 三类事件一律在捕获阶段拦下不外传，空格只当空格用，方向键只当光标移动；`Enter` 直接确认、`Escape` 直接取消。
- **关键词卡片盖住整页**：改弹窗为工作台内联卡片（见下），顺带消除了弹窗打开期间快捷键在后台生效的隐患。

### 调整

- **订阅关键词输入从弹窗改为工作台内联卡片**：原先点「订阅该 UP」会弹一个盖满整页的遮罩层，用户看不到当前视频 / UP 的信息。现在改为在工作台里就地插一张卡片 —— **位置紧贴触发它的那张卡片下方**，所见即所得；点「开始订阅」或「取消」后卡片自动移除。卡片同样会列出该 UP 已订阅过的关键词组合，避免建重复。
- **合集 / 专辑页下载区默认折叠**：合集、专辑动辄几十上百集，全量列表铺开会把工作台撑成一条长柱，把页面本身挡死。现在默认只显示一张**资料卡**（封面 / 名称 / 集数 / 「订阅该合集」），下载列表收在「下载」按钮后面，点了才展开。展开状态会被记住 —— 缓存重建 DOM 后不会悄悄折回去；展开时自动全选并同步「下载所选 (N)」计数，支持再点「收起」。

### 说明

- 下载区的列表 HTML 是**预先渲染好**的，展开 / 收起只切 `display`，不会重新请求接口。

## [0.3.6] — 2026-09-21

### 新增

- **同一 UP 可用不同关键词重复订阅**：订阅去重规则从「按 mid 唯一」改为「按 mid + 关键词组合唯一」。关键词组合不同的两组订阅可以并存（例如「只看攻略」与「只看直播回放」），各自独立筛选、独立刷新；关键词组合完全相同则判为重复，直接刷新已有那条。判定忽略输入顺序，排除关键词也参与判定。
- **订阅弹层显示已订阅的关键词组合**：该 UP 已有订阅时，关键词输入框下方列出已订阅过的组合，便于判断还缺哪一组。
- **订阅列表区分同 UP 多条订阅**：同一 UP 的多条订阅带「第 n/m 条」小标记，避免列表出现两行同名卡片无法分辨。

### 调整

- **「编辑筛选关键词」增加冲突校验**：改后的关键词组合若与该 UP 另一条订阅完全相同，拒绝保存并提示。

### 测试

- 第 6 项真机回归「UP 订阅」由 11 断言扩到 **22 断言**，新增多关键词重复订阅、相同关键词判重、订阅 id 独立、弹层列出已有组合、「第 n/m 条」标记、跨订阅条目不串号，以及全库去重语义（先到先得）的回归。

### 说明

- 稍后再看是**全库去重**的：一条投稿只保留一条记录。多条订阅命中同一投稿时按**先到先得**归属，哪个订阅先导入就归它，后续订阅刷新时不会重复导入，列表因此不会膨胀出重复条目。

## [0.3.5] — 2026-09-21

### 新增

- **视频页工作台识别「合集」**：视频页若检测到该视频属于某个合集（`ugc_season`），会额外渲染一张合集卡 —— 显示合集封面、名称、UP 主与视频数，并提供「订阅该合集」按钮，点击即订阅整季并把合集内全部视频导入稍后再看；订阅后每次刷新自动补齐新增分集。
- **视频页「加入稍后再看」按钮**：工作台主卡新增该按钮，把当前视频直接收进稍后再看。多分P视频每条命名为 `分P名 - 视频标题`，并尊重分P勾选状态；已在列表中的条目自动跳过并在提示中给出条数。
- **设置：下载线程数可调**：新增「下载线程数」输入框（2 ~ 16，默认 8），调节视频轨多线程分块下载的并发数。内部仍保留「每连接至少分摊 4MiB」的安全线，小文件不会因为设了大线程数而真的开满连接。
- **设置：仅下载音频时可选音质**：新增「音频音质」下拉框，最高支持 **Hi-Res 无损**（30251），另含杜比全景声（30250）、192K（30280）、132K（30232）、64K（30216）。取轨策略为「不超过所选档位的最高可用档」，杜比 `ec-3` 与无损 `flac` 编解码标识已纳入匹配。

### 调整

- **移除工作台左下角「下载所选 (N)」灰色小字**，计数统一只在右下角下载按钮上显示。

### 移除

- **设置中的「新订阅回填条数」已删除**。新订阅统一按完整口径导入：UP 订阅最多拉 50 条、合集/专辑最多 20 页、番剧导入全部剧集。

### 测试

- 第 9 项真机回归「合集/专辑卡片」由 15 断言扩到 **30 断言**，新增「视频页识别所属合集（`ugc_season`）」与「加入稍后再看命名规则」两组覆盖。

## [0.3.4] — 2026-09-21

### 修复

- **合集/专辑页工作台不显示**：B 站新版合集地址是 `space.bilibili.com/<uid>/lists/<sid>`（sid 在路径里），旧代码只从 `?sid=` 取，导致整张卡片出不来（没有封面、没有订阅按钮）。现已同时支持三种地址形态：`/channel/collectiondetail?sid=`、`/lists?sid=`、`/lists/<sid>`。
- **UP 订阅刷新失败时静默无提示**：空间投稿接口被风控（412）且旧版兜底接口也失效时，刷新会静默返回 0 条，用户完全看不出是「本来就没更新」还是「接口挂了」。现在会在界面提示失败原因（同一订阅 60 秒内只提示一次）。

### 新增

- **专辑（系列）订阅支持**：`?type=series` 的专辑走独立接口（`x/series/archives`），此前完全不支持。现在可订阅、导入全部视频、刷新自动补齐新增分P，订阅列表里类型标签显示「专辑」。
- **合集/专辑卡片显示封面**：工作台卡片补齐封面图、名称、集数与「订阅该合集 / 订阅该专辑」按钮。

### 测试

- 质量门扩到 9 项，新增「合集/专辑卡片」真机回归（路径式 sid 解析 / 封面渲染 / 专辑接口 / 订阅全量导入），15/15。

## [0.3.3] — 2026-09-20

### 新增

- **取地址失败自动重试**：网络不好时，下载在「获取播放地址」阶段失败不再直接判死，任务自动回队重试（至多 3 次），每次重试有 toast 与任务行提示；重试期间不占用下载通道。大会员/付费/地区限制等业务性错误仍直接提示，不浪费重试。
- **下载等待阶段文案更明确**：任务出队后显示「获取播放地址…」，不再是含糊的「处理中… 2%」（该百分比原为固定占位值，与真实进度无关）。

### 调整

- **接口重试加码**：普通接口失败重试 2 次加到 3 次（共 4 次尝试），番剧播放地址接口重试 1 次加到 2 次；配合回队重试，弱网下取地址阶段最多可扛十几次请求重试。
- **请求超时放宽到 120 秒**：接口/文本类请求的强制超时由 20 秒放宽到 120 秒，下载引擎文件由 30 秒放宽到 120 秒——慢网不再被过早掐断。视频/音频数据传输保持不限时（由连接状态自然报错）。

## [0.3.2] — 2026-09-18

### 新增

- **下载失败提示更友好**：toast 现在显示一句简短的报错摘要（如「网络超时」「HTTP 403」「网络连接失败」「下载引擎加载失败(可改用 ≤720P)」），不再甩出大段技术错误堆栈；完整错误原文仍保留在「下载」页的任务记录里。

## [0.3.1] — 2026-09-15

### 修复

- **稍后再看时长显示 00:00**：UP 投稿接口返回的时长是 `"5:30"` 这类 `mm:ss` 字符串，被当作秒数解析后归零。现在统一归一化，新订阅的 UP 条目时长正常。
- **UP 空间页 mid 末位被截断**：`space.bilibili.com/<mid>`（无尾斜杠，最常见的地址形态）打开工作台时，脚本取 mid 的方式会削掉最后一位数字，导致订阅 / 监控到错误的 UP。改用正则提取。
- **存量数据自愈**：已入库的 00:00 条目会在下次 UP 订阅刷新时自动用真实时长回填，无需手动清理。

### 测试

- 质量门扩到 8 项，新增「UP 订阅时长」真机回归（真实 UP 订阅 → 时长校验 → 人为损坏 → 刷新自愈），11/11。

## [0.3.0] — 2026-09-15

### 新增：番剧支持

- 打开任意番剧页（`/bangumi/play/ss*` 或 `/bangumi/play/ep*`），工作台自动识别，ep 链接会自动反查所属季。
- 解析并展示 **封面 / 番剧名 / 简介 / 分节剧集列表**（正片、主题曲、花絮等分区显示，每条带集名与时长）。
- 一键 **订阅整季**：订阅后全部剧集（含 OP/ED）一次性导入稍后再看，之后刷新只补新增集；订阅类型标签为「番剧」，筛选条新增「番剧」档。
- **按集勾选下载**，支持全选/反选；弹幕 XML 一并下载。
- 下载策略：番剧大多数情况直接出单文件 mp4 免转码；只有请求 1080P+ / HDR 时才回落到分片合并。
- 番剧条目在稍后再看里带「正片 / 主题曲」角标，点击打开对应剧集页。

### 新增：下载设置三项开关

- **附带封面文件**：下载视频时同目录保存一张同名封面图（番剧取剧集封面，普通视频取稿件封面）。
- **音视频分离**：跳过合并，直接保存 `.video.m4s` + `.audio.m4s` 两个原始分片（适合自己后期处理）。开启后不再使用直链。
- **仅下载音频**：只保存 `.m4a` 音轨（192K），不下载视频。与「音视频分离」互斥，开启任一项会自动关闭另一项。
- 分离 / 仅音频模式下自动跳过合并引擎预载，不再无谓拉取引擎文件。

### 新增：视频轨多线程下载

- 分片下载模式下视频轨自动按字节分块 **8 连接并发**（按文件大小自适应 2~8 线程），音频轨保持单线程。
- 单分片失败自动重试（3 次）；CDN 不支持范围请求或重试耗尽时**自动回退单线程**，不影响任务完成。
- 分离模式分块直接落盘不占双倍内存；合并模式在交给引擎前按序拼接。

### 修复

- 番剧卡片「取消全选」后，右下角「下载所选」按钮计数与实际勾选数不同步（原先只更新左侧汇总）。
- 补回 `subscribeUp` / `subscribeSeasonNow` / `askSubKw` / `bgFinishSub` / `monAfterAdd` —— 前端重写时整块丢失，导致工作台「订阅该 UP」「订阅该合集」按钮点击报 `ReferenceError`。关键词弹窗改用自定义弹层（不再用原生 `prompt`）。
- 稍后再看的下载按钮跳转修正（原先跳到旧页签名 `tool`，现在跳「下载」页）。
- 订阅页 / 监控页空态残留的「工具」旧称改为「工作台」。
- 存储层对 `subs` / `items` / `mons` / `dls` / `dedupe` / `ignore` 做严格数组校验，避免异常数据导致启动崩溃。
- 仅音频模式下弹幕文件名错误带 `.m4a` 后缀（`xx.m4a.xml` → `xx.xml`）。
- 音视频分离的音频落盘增加异常保护，失败时明确提示（原先静默失败）。

### 安全

- 移除番剧下载请求 URL 中的 `session` 登录态参数：登录鉴权完全依赖浏览器对 B 站自动携带的 Cookie（SESSDATA 为 HttpOnly，原参数实际恒为空），凭据不再出现在请求 URL 中。

### 变更：权限与注入范围收敛

- 移除从未调用的 `@grant GM_listValues`。
- 移除 7 条无用 `@connect`：`live` / `bangumi` / `manga.bilibili.com`、`bilibili.cn`，以及仅用 `fetch` 访问的第三方 CDN `registry.npmmirror.com` / `cdn.jsdelivr.net` / `unpkg.com`（`@connect` 只约束 `GM_xmlhttpRequest` 与 `GM_download`，`fetch` 不受其限制）。
- 移除 3 条无页面逻辑的 `@match`：`live` / `t` / `manga.bilibili.com`。脚本只在 `www.bilibili.com` 与 `space.bilibili.com` 注入。

## [0.2.0] — 2026-09-12

### 界面重设计

- 左侧栏改为 6 个独立页签：**稍后 / 工作台 / 监控 / 订阅 / 下载 / 设置**，全部使用 Bootstrap Icons 实心图标，界面内不再有 emoji。
- 原「工具箱」更名 **工作台**（第 2 位），只负责当前页面的一键订阅 / 监控 / 下载。
- **下载管理**从工作台里拆出来，独立成页（第 5 位），带任务数量角标。

### 性能

- 列表改为按 key 差分渲染，节点复用、零 DOM 移动。
- rAF 合帧 + 视图脏标记，只重绘当前可见页。
- 事件委托：每个列表根节点只绑一次监听，长期开着不累积。
- 惰性渲染 + 滚动分页；离屏卡片用 `content-visibility` 跳过布局与绘制。
- 属性级写入缓存：值没变就不写 DOM。

### 修复

- 修复存储里 `dedupe` 等字段为非数组时启动崩溃、整个面板挂不上的问题。
- 修复头像本地缓存层（`faceSrc` / `warmAva`）缺失导致订阅页渲染报错、列表为空的问题。
- 修复极早期注入（`document-start`）时 `documentElement` 尚未就绪导致挂载崩溃的问题。

## [0.1.29] — 初始可用版本

- UP 投稿 / 合集 / 分P 视频三种订阅、增量监控、稍后再看、下载助手的基础形态。
