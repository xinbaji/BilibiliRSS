# BilibiliRSS v0.3.8

下载不再「一次抖动就废掉整个任务」。这一版把下载链路的失败处理从「判死」改成「自愈」。

## 修复

- **下载中断后整个任务被判死**
  视频轨多线程失败会回退单线程，但**回退之后再失败就没有任何挽救** —— 一次网络抖动就红字报「网络超时」，任务直接作废。根因是重试逻辑只覆盖「还没拿到播放地址」那一段：一旦 `planDone` 打上标记，后续任何失败都直接判死。
  现在**媒体下载阶段也会自动回队重试**（至多 2 次），并且重试时**重新取一次播放地址** —— B 站 `playurl` 给出的 CDN 链接是带时效的，排队久了会过期，重取正好一并解决。只有大会员 / 付费 / 地区限制这类业务性错误才直接判死。

## 调整

- **多线程失败改为「逐级降并发」**
  原先任一块失败就一步退回单线程：速度砍到 1/8，还白扔掉已经下好的分块。现在按 `用户线程数 → 一半 → … → 2` 逐档重试 —— 多数「并发太高被 CDN 限流」的情况降到一半就通了；各档都不行才回退单线程。

- **失败时掐掉还在跑的兄弟请求**
  多线程里只要有一块判负，其余 Range 请求会被主动 `abort`。以前它们会在后台继续占着 CDN 的并发额度，和紧接着的重试抢同一批连接 —— 互相拖死，还白烧流量。同时失败后不再让残留回调继续写进度，进度条不再乱跳。

- **分块重试加退避 + 分类**
  尝试次数 3 → 4，间隔 400ms / 800ms / 1600ms 递增。以前是零间隔连撞，被限流时几乎必然继续失败。`HTTP 4xx`（403 防盗链、404 地址过期等）不再做无谓重试；`408` / `429` 仍会重试。

- **「超时」不再含糊**
  原来的 `timeout: 0` 配 `ontimeout: () => '超时'`，会把「连接被掐断」「响应没读完」统统报成一个「超时」，看不出到底是没下文还是真超时。现在改为**停滞看门狗** —— 只在 120 秒内一个字节都没涨时才判失效，错误信息带上已收字节数（`停滞 12.3MB 无数据`）；分块错误还会带上是哪一块（`超时 · 块 0-10357453`）。

## 说明

- 看门狗盯的是「有没有进展」，不是「总时长」。有进展就一直重置计时，所以慢链路的长下载不会被误杀 —— 这是刻意的，不是超时放宽。

## 升级说明

覆盖安装即可，配置、订阅、稍后再看数据全部保留。
